# Core LM Implementation Packet

Use this packet when Core LM routes a canonical task to an implementation agent.

Required handling:

- Treat ledger and provenance references as the source of truth.
- Preserve branch isolation and append-only state behavior.
- Implement the requested change with focused tests.
- Report verification commands and any remaining blockers.
